# Test Environment Configuration
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=test

# Firebase Auth (mock values for testing)
FIREBASE_PROJECT_ID=test-project
FIREBASE_CREDENTIALS_PATH=./test-firebase-credentials.json

# Google Cloud (mock values for testing)
GCP_PROJECT_ID=utxoiq-test
BIGQUERY_DATASET_INTEL=intel_test
BIGQUERY_DATASET_BTC=btc_test

# Cloud Storage
ARCHIVE_BUCKET_NAME=utxoiq-test-archives

# Cloud SQL (PostgreSQL) - Docker container on different port
CLOUD_SQL_CONNECTION_NAME=local:test:instance
DB_USER=utxoiq
DB_PASSWORD=utxoiq_dev_password
DB_NAME=utxoiq_db
DB_HOST=localhost
DB_PORT=5433

# Database Connection Pool
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Redis - Docker container on different port
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=

# Stripe (test keys)
STRIPE_SECRET_KEY=sk_test_mock_key
STRIPE_WEBHOOK_SECRET=whsec_test_secret

# Vertex AI
VERTEX_AI_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-pro

# Rate Limiting
RATE_LIMIT_FREE_TIER=100
RATE_LIMIT_PRO_TIER=1000
RATE_LIMIT_POWER_TIER=10000
RATE_LIMIT_WINDOW=3600

# CORS
CORS_ORIGINS=http://localhost:3000

# WebSocket
WS_HEARTBEAT_INTERVAL=30
WS_MAX_CONNECTIONS=10000
